import { MockDatabase } from "../types";

// Mock database to simulate server-side storage
export const mockDatabase: MockDatabase = {
  users: {},
  words: ["Dragon", "Unicorn", "Phoenix", "Griffin", "Wizard", "Knight"],
  elos: {},
};
